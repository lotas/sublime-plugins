
#Goto Commit

Plugin looks via blame author of the line and opens corresponding commit in github


Howto add:

Preferences - Key Bindings
Add trigger:

{ "keys": ["ctrl+f1"], "command": "goto_commit" }

Copy gotocommit.py -> ~/config/sublime-text-2/Packages/User/

